// Primitive Data Types -> Dynamic typing (type of value is inferred and can be changed)

// Numbers -> always floating point numbers
let age = 23;

// Strings
let name = "Tanya";

// Boolean - logical type
let isDeveloper = true;

// Undefined - taken by variable that's not defined yet;
let children;
console.log(typeof children); // undefined

// Null - means empty value
const nothing = null;

// Symbol - unique and cannot be changed

// BigInt - ES2020; larger integers than the Number type can hold
