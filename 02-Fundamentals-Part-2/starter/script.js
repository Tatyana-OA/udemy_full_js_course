// "use Strict";
// // Activate Strict Mode -> better for debugging (visible errors in console)

// let hasDriversLicence = false;
// const passTest = true;

// if (passTest) {
//   hasDriversLicence = true; // if we misspell, strict Mode will help
// }

// KEY PRESS EVENTS

// document.addEventListener("keydown", (e) => {
//   console.log("Key pressed");
//   console.log(e.code === "Escape" ? "Hit escape key" : "Not escape key");
// });
